export const breakpoints = {
  xs: `(max-width: ${400 / 16}em)`,
  sm: `(max-width: ${600 / 16}em)`,
  md: `(max-width: ${900 / 16}em)`,
  lg: `(max-width: ${1400 / 16}em)`,
  xl: `(max-width: ${1800 / 16}em)`,
};
